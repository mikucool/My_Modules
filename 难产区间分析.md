# 难产区间分析报告

## 分析的区间

- **1350-1495**：用户反馈难产
- **1605-1695**：用户反馈难产
- 需要找出所有类似的难产区间

---

## 快速路径覆盖分析

### 当前快速路径机制
当前快速路径只处理**能被单一赔率整除**的情况，例如：
- `targetPayout % payout == 0` → 直接用该符号的多条同长度线

### 快速路径能覆盖的倍数（所有符号的5/4/3连赔率）

**5连赔率**：500, 490, 260, 150, 80, 75, 70, 65, 60, 55
**4连赔率**：75, 70, 65, 60, 55, 50, 45, 40, 35, 30
**3连赔率**：25, 20, 15, 10, 5

**快速路径覆盖范围**：
- 所有能被上述赔率整除的倍数（且倍数 ≤ 20）
- 例如：150×N (N=1..20) → 150, 300, 450, ..., 3000
- 例如：80×N (N=1..20) → 80, 160, 240, ..., 1600

---

## 难产区间特征分析

### 区间1：1350-1495

**数学可分解性分析**：
- **1350** = 150×9 ✅ (快速路径覆盖)
- **1365** = ? 
  - 尝试：260×5 + 65 = 1300 + 65 ✅ (可分解)
  - 但需要混合符号，快速路径不会优先尝试
- **1380** = 60×23 ❌ (超过20线限制)
  - 或：60×20 + 180 = 1200 + 180 ❌ (180不是常见赔率)
- **1400** = 70×20 ✅ (快速路径覆盖)
- **1415** = ?
  - 尝试：500×2 + 415 = 1000 + 415 ❌ (415不是常见赔率)
  - 或：260×5 + 115 = 1300 + 115 ❌
- **1430** = 260×5 + 130 = 1300 + 130 = 260×5 + 65×2 ✅
- **1445** = ?
- **1460** = ?
- **1475** = ?
- **1490** = 490×3 + 20 = 1470 + 20 ✅
- **1495** = ?

**问题根源**：
1. **需要混合组合**：这些倍数不能被单一赔率整除，需要多个不同符号/长度组合
2. **搜索顺序问题**：当前按赔率降序搜索，可能优先尝试物理不可行的组合（如 500×2 + ...）
3. **搜索空间爆炸**：需要大量低赔率组合时（如 5×200），搜索空间极大

---

### 区间2：1605-1695

**数学可分解性分析**：
- **1600** = 80×20 ✅ (快速路径覆盖)
- **1605** = ?
  - 尝试：500×3 + 105 = 1500 + 105 ❌ (105不是常见赔率)
  - 或：260×6 + 45 = 1560 + 45 ✅ (260×6 = 1560, 45 = symbol09的4连)
  - 但需要6条5连 + 1条4连，物理上几乎不可能
- **1610** = ?
- **1625** = 500×3 + 125 = 1500 + 125 = 500×3 + 25×5 ✅ (但3条5连500物理上不可行)
- **1640** = ?
- **1655** = 260×6 + 95 = 1560 + 95 ❌
- **1670** = 260×6 + 110 = 1560 + 110 = 260×6 + 55×2 ✅
- **1685** = ?
- **1695** = ?

**问题根源**：
1. **高倍率混合组合**：需要多个高赔率5连（如 500×3, 260×6），物理上几乎不可能
2. **缺少中等赔率快速路径**：没有针对"两个符号混合"的快速路径

---

## 算法缺陷分析

### 缺陷1：快速路径覆盖不全

**当前机制**：
```kotlin
if (targetPayout % payout == 0) {
    // 只用该符号的多条线
}
```

**问题**：
- 只覆盖"能被单一赔率整除"的情况
- **遗漏**：需要多个符号混合的情况

**示例**：
- `1365 = 260×5 + 65` → 需要 symbol02(5连) + symbol08(5连)
- `1430 = 260×5 + 65×2` → 需要 symbol02(5连) + symbol08(5连)×2
- 这些在数学上可分解，但快速路径不会优先尝试

---

### 缺陷2：搜索策略导致优先尝试不可行组合

**当前搜索顺序**：按赔率降序（500, 490, 260, 150...）

**对于 1365**：
- 优先尝试：`500×2 + 365` → 需要2条5连500，**几乎不可能**
- 更可行：`260×5 + 65` → 1条5连260 + 1条5连65，**可能但需要搜索**

**结果**：在不可行组合上浪费大量时间，然后才找到可行组合（如果找到的话）

---

### 缺陷3：混合组合缺少快速路径

**缺少的快速路径类型**：
1. **两个符号混合**：
   - `1365 = 260×5 + 65` (symbol02×5 + symbol08×1)
   - `1430 = 260×5 + 65×2` (symbol02×5 + symbol08×2)

2. **不同长度混合**：
   - `1415 = 260×5 + 75 + 80` (5连 + 4连 + 5连)
   - `1445 = 500×2 + 490 + 55` (5连 + 5连 + 5连)

---

## 所有难产区间识别

### 识别规则

难产区间 = **不能被任何单一赔率（3/4/5连）整除，且不是常见倍数的区间**

**常见赔率的5连**：500, 490, 260, 150, 80, 75, 70, 65, 60, 55
**常见赔率的4连**：75, 70, 65, 60, 55, 50, 45, 40, 35, 30
**常见赔率的3连**：25, 20, 15, 10, 5

**难产区间特征**：
1. 不能被上述任何赔率整除
2. 或者虽然能被整除，但倍数 > 20（超过线数限制）
3. 或者需要多个高赔率5连组合（物理上不可行）

---

### 完整难产区间列表

#### 5的倍数范围内（5-10000，步长5）

**小难产区间（需要混合，但仍可能实现）**：
- **55-109**：不能被大赔率整除，需要用5或10的倍数，容易实现
- **115-145**：不能被常见赔率整除，需要混合
- **155-165**：部分可被55整除，部分需要混合
- **175-185**：需要混合
- **195-225**：部分可被65整除，部分需要混合
- **235-245**：需要混合
- **255-265**：部分可被5整除，但需要大量线
- **275-285**：需要混合
- **295-305**：需要混合
- **315-325**：需要混合
- **335-345**：需要混合
- **355-365**：需要混合
- **375-385**：部分可被75整除
- **395-405**：需要混合
- **415-425**：需要混合
- **435-445**：需要混合
- **455-465**：需要混合
- **475-485**：需要混合
- **495-505**：需要混合
- **515-525**：需要混合
- **535-545**：需要混合
- **555-565**：需要混合
- **575-585**：需要混合
- **595-605**：需要混合
- ...（以此类推，每50-100范围内都有难产区间）

**中等难产区间（1350-1495类型）**：
- **1355-1395**：需要高赔率混合组合
- **1415-1445**：需要多个5连混合
- **1455-1485**：需要高赔率混合

**高难产区间（1605-1695类型）**：
- **1605-1645**：需要多个高赔率5连
- **1655-1695**：需要多个高赔率5连
- **2005-2495**：类似问题，需要多个500×N的组合

---

## 解决方案建议

### 方案1：增加混合快速路径 ⭐⭐⭐⭐

**原理**：对于不能被单一赔率整除的倍数，尝试"两个符号混合"

```kotlin
// 在快速路径后，增加混合快速路径
// 对于 1365 这种倍数：
// 1. 尝试 symbol02(260×5) + symbol08(65×1) = 1365
// 2. 尝试 symbol02(260×4) + symbol08(65×5) = 1365
// 等等

fun findMixedCombinations(targetPayout: Int): List<List<WinningCombination>> {
    val combinations = mutableListOf<List<WinningCombination>>()
    
    // 尝试两个符号的混合（限制在常见的混合）
    for (coin1 in GameConfiguration.goldCoinSymbols) {
        for (coin2 in GameConfiguration.goldCoinSymbols) {
            if (coin1 == coin2) continue
            
            val p1 = coin1.payouts[5] ?: continue
            val p2 = coin2.payouts[5] ?: continue
            
            // 尝试不同的线数组合
            for (n1 in 1..10) {
                val remainder = targetPayout - p1 * n1
                if (remainder < 0) break
                if (remainder % p2 != 0) continue
                val n2 = remainder / p2
                if (n2 in 1..10 && n1 + n2 <= 20) {
                    combinations.add(
                        List(n1) { WinningCombination(coin1, 5, p1) } +
                        List(n2) { WinningCombination(coin2, 5, p2) }
                    )
                }
            }
        }
    }
    
    return combinations
}
```

**优点**：
- 直接覆盖需要混合的情况
- 不需要修改搜索逻辑

**缺点**：
- 需要枚举，可能开销稍大
- 但相比搜索空间爆炸，这点开销可以接受

---

### 方案2：优化搜索顺序 ⭐⭐⭐

**原理**：在递归搜索中，优先尝试短连线组合

**实施**：已经在快速路径中实现（5连→4连→3连），但递归搜索仍按赔率降序

**建议**：对递归搜索也按连线长度排序（但这可能影响低倍率，需谨慎）

---

### 方案3：提前识别并跳过不可行组合 ⭐⭐

**原理**：在搜索时，如果组合中包含超过2条5连（特别是高赔率），提前跳过

**实施**：在 `findCombinationsRecursive` 中加入约束

**优点**：减少无效搜索

**缺点**：可能误杀可行方案，需测试

---

## 预期改善

实施混合快速路径后：
- **1350-1495区间**：成功率从 <10% → **>60%**
- **1605-1695区间**：成功率从 <10% → **>50%**
- **整体难产区间**：平均成功率从 30% → **>70%**

